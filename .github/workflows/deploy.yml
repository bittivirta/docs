name: "Deploy to GitHub repository docs-engine"

on:
    push:
        branches:
            - main

jobs:
    deployment:
        runs-on: ubuntu-latest
        steps:
            - name: Setup SSH
              uses: MrSquaare/ssh-setup-action@v1
              with:
                  host: github.com
                  private-key: ${{ secrets.SSH_PRIVATE_KEY }}

            - name: "Prepare working dir"
              run: |
                  ls -la
                  mkdir docs-engine
                  cd docs-engine
                  ls -la

            - name: Clone repository
              run: |
                  git clone git@github.com:bittivirta/docs-engine.git .
                  ls -la

            # - name: Install Node.js
            #   uses: actions/setup-node@v3
            #   with:
            #       node-version: 18

            # - uses: pnpm/action-setup@v2
            #   name: Install pnpm
            #   id: pnpm-install
            #   with:
            #       version: 8
            #       run_install: false

            # - name: Get pnpm Store Directory
            #   id: pnpm-cache
            #   shell: bash
            #   run: |
            #       echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

            # - uses: actions/cache@v3
            #   name: Setup pnpm cache
            #   with:
            #       path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
            #       key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
            #       restore-keys: |
            #           ${{ runner.os }}-pnpm-store-

            # - name: Install Dependencies
            #   run: pnpm install

            # - name: "Bump version"
            #   run: |
            #       pnpm bump

            # - name: "Commit changes"
            #   run: |
            #       git config --local user.email "ci@noreply.bittivirta.fi"
            #       git config --local user.name "Bittivirta CI"
            #       git add .
            #       git commit -m "ci: :bookmark: Bump version"

            # - name: "Push changes"
            #   run: |
            #       git push
